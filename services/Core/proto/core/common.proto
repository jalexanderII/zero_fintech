syntax = "proto3";

package core;
option go_package = "./proto;core";

enum DELETE_STATUS {
  STATUS_UNKNOWN = 0;
  PENDING = 1;
  IN_PROGRESS = 2;
  SUCCESS = 3;
  FAILED = 4;
}

enum PlanType {
  PLANTYPE_UNKNOWN = 0;
  OPTIM_CREDIT_SCORE = 1;
  MIN_FEES = 2;
}

enum PaymentFrequency {
  PAYMENTFREQ_UNKNOWN = 0;
  WEEKLY = 1;
  BIWEEKLY = 2;
  MONTHLY = 3;
  QUARTERLY = 4;
}